---
name: 1c-platform-docs
description: "MCP-сервер bsl-platform-help для доступа к документации API платформы 1С (keyword/semantic/hybrid поиск). Применять для проверки существования встроенных методов/свойств, получения сигнатур и членов типов."
---

# 1C:Enterprise — Документация платформы 1С (MCP)

## Описание

MCP-сервер **bsl-platform-help** — доступ к документации API платформы 1С:Предприятие. Поддерживает keyword, semantic (эмбеддинги) и hybrid поиск. Проверка существования встроенных функций/методов/свойств, получение сигнатур, просмотр членов типа.

## Когда использовать

- Проверка существования встроенных процедур/функций/методов/свойств
- Поиск методов и типов по описанию на естественном языке
- Получение сигнатуры и параметров метода
- Просмотр всех членов типа (методы + свойства)
- Получение конструкторов типа
- Проверка версии платформы

## Инструкции

### `search` — Поиск по документации (основной)

| Параметр | Обязательный | Описание |
|----------|:------------:|----------|
| `query` | да | Запрос (рус/англ, CamelCase, natural language) |
| `mode` | нет | `keyword` / `semantic` / `hybrid` (default: hybrid) |
| `type` | нет | Фильтр: `method`, `property`, `type` |
| `limit` | нет | Максимум результатов (1-50, default 10) |

```bash
# Точный keyword-поиск
search("ТекущаяДата")

# Семантический поиск
search("как записать файл на диск", mode="semantic")

# Гибридный поиск
search("HTTP запрос POST", mode="hybrid")
```

### `info` — Детальная информация

| Параметр | Обязательный | Описание |
|----------|:------------:|----------|
| `name` | да | Точное имя (рус/англ): `НайтиПоСсылке`, `FindByRef` |
| `type` | да | `method`, `property`, `type` |

### `get_member` — Метод/свойство типа

| Параметр | Обязательный | Описание |
|----------|:------------:|----------|
| `type_name` | да | Имя типа: `ТаблицаЗначений`, `ValueTable` |
| `member_name` | да | Имя метода/свойства |

### `get_constructor` — Конструктор типа

| Параметр | Обязательный | Описание |
|----------|:------------:|----------|
| `type_name` | да | Имя типа |

## Примеры

```bash
# Проверить, есть ли метод ЗаписатьJSON
search("ЗаписатьJSON")

# Получить сигнатуру метода
info(name="ЗаписатьJSON", type="method")

# Все свойства типа ТаблицаЗначений
get_member(type_name="ТаблицаЗначений", member_name="")

# Конструктор HTTPСоединение
get_constructor(type_name="HTTPСоединение")
```

## Лучшие практики

- **Используй `hybrid`** (по умолчанию) — лучший баланс точности и полноты
- **Для проверки существования** — `keyword` мод быстрее
- **Для неизвестных имён** — `semantic` мод найдёт по описанию
- **Офлайн** — bsl-platform-help работает локально, без интернета

## Ссылки

- [claude-code-skills-1c (Desko77)](https://github.com/Desko77/claude-code-skills-1c) — исходный скилл 1c-platform-docs
- [bsl-platform-help MCP](https://github.com/nickolay-grechkin/bsl-platform-help) — сервер документации
